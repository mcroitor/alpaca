ARG PYTHON_VERSION=3.9

FROM python:${PYTHON-VERSION}-alpine

ENV DSN=sqlite:///data/app.db

ENV APP_DIR=/app

WORKDIR ${APP_DIR}

VOLUME /data

COPY ./src/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY ./src .

CMD ["python", "/app/app.py"]